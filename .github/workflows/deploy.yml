name: Deploy to masterc.cn

on:
  push:
    branches: [master]
  workflow_dispatch:

concurrency:
  group: deploy-masterc
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        run: |
          cd frontend
          npm ci
          npm run build

      - name: Pack release bundle
        run: |
          mkdir -p release/backend release/frontend-dist release/deploy
          rsync -a --delete \
            --exclude venv \
            --exclude __pycache__ \
            --exclude .env \
            backend/ release/backend/
          rsync -a --delete frontend/dist/ release/frontend-dist/
          rsync -a deploy/ release/deploy/
          tar czf release.tar.gz -C release .

      - name: Upload release
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: release.tar.gz
          target: /tmp

      - name: Apply release on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -euo pipefail
            APP_DIR=/opt/dorm-power-guard-lite
            mkdir -p "$APP_DIR/backend" "$APP_DIR/frontend/dist" "$APP_DIR/deploy"
            mkdir -p /tmp/dorm-release
            tar xzf /tmp/release.tar.gz -C /tmp/dorm-release
            rsync -a /tmp/dorm-release/backend/ "$APP_DIR/backend/"
            rsync -a --delete /tmp/dorm-release/frontend-dist/ "$APP_DIR/frontend/dist/"
            rsync -a /tmp/dorm-release/deploy/ "$APP_DIR/deploy/"
            cd "$APP_DIR/backend"
            if [ ! -d venv ]; then python3 -m venv venv; fi
            ./venv/bin/pip install --upgrade pip -q
            ./venv/bin/pip install -r requirements.txt -q
            cp "$APP_DIR/deploy/systemd/dorm-backend.service" /etc/systemd/system/
            cp "$APP_DIR/deploy/systemd/dorm-nonebot.service" /etc/systemd/system/
            cp "$APP_DIR/deploy/nginx/masterc.cn.conf" /etc/nginx/sites-available/masterc.cn
            ln -sf /etc/nginx/sites-available/masterc.cn /etc/nginx/sites-enabled/masterc.cn
            systemctl daemon-reload
            systemctl restart dorm-backend dorm-nonebot
            nginx -t
            systemctl reload nginx
            curl -fsS https://masterc.cn/health
            rm -rf /tmp/dorm-release /tmp/release.tar.gz
            echo "Deploy success"
